<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SBMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SBMAUI"
    xmlns:views="clr-namespace:SBMAUI.Views.SignAndCreate"
    Title="SBMAUI"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent ContentTemplate="{DataTemplate views:SignIn}" Route="Sign in" />

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

</Shell>